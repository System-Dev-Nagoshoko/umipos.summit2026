---
interface Props {
	title: string;
}

const { title } = Astro.props;
const baseUrl = import.meta.env.BASE_URL;
---

<!doctype html>
<html lang="ja">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/png" href={`${baseUrl}favicon.png`} />
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;500;700;900&display=swap" rel="stylesheet">
		<title>{title}</title>
	</head>
	<body>
		<header class="site-header">
			<div class="container header-container">
				<a href={baseUrl} class="logo-link">
					<img 
						src={`${baseUrl}logo.png`} 
						alt="Logo" 
						class="logo-img" 
					/>
					<span class="logo-text">Nago Youth Future Summit</span>
				</a>

				<nav class="nav">
					<a href={baseUrl} class="nav-item">TOP</a>
					<a href={`${baseUrl}about/`} class="nav-item">ABOUT</a>
					<a href={`${baseUrl}sns/`} class="nav-item">SNS</a>
					<a href={`${baseUrl}contact/`} class="nav-item nav-item-cta">CONTACT</a>
				</nav>
			</div>
		</header>

		<main class="main-content">
			<slot />
		</main>

		<footer class="site-footer">
			<div class="container footer-container">
				<p class="copyright">&copy; 2026 Nago Youth Future Summit & うみぽす甲子園ファイナリストの企画イベント All rights reserved.</p>
			</div>
		</footer>
	</body>
</html>

<style is:global>
	:root {
		--primary: #0284c7;
		--primary-hover: #0369a1;
		--accent-bg: #f0f9ff;
		--text: #1e293b;
		--text-muted: #64748b;
		--bg: #fafafa;
		--border: #e2e8f0;
		--shadow-sm: 0 4px 20px rgba(0, 0, 0, 0.05);
	}

	* {
		box-sizing: border-box;
		margin: 0;
		padding: 0;
	}

	html, body {
		width: 100%;
		overflow-x: hidden;
	}

	body {
		font-family: 'Noto Sans JP', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
		color: var(--text);
		background: var(--bg);
		line-height: 1.7;
		letter-spacing: 0.03em;
		display: flex;
		flex-direction: column;
		min-height: 100vh;
	}

	.main-content {
		flex: 1;
	}

	.container {
		width: 100%;
		max-width: 1100px;
		margin: 0 auto;
		padding: 0 1.5rem;
	}

	/* ヘッダー構造（ガラスモフィズムと浮かび上がるようなシャドウ） */
	.site-header {
		background: rgba(255, 255, 255, 0.85);
		backdrop-filter: blur(12px);
		-webkit-backdrop-filter: blur(12px);
		border-bottom: 1px solid rgba(226, 232, 240, 0.8);
		position: sticky;
		top: 0;
		z-index: 50;
		transition: all 0.3s ease;
	}

	.header-container {
		display: flex;
		justify-content: space-between;
		align-items: center;
		height: 72px;
	}

	.logo-link {
		display: flex;
		align-items: center;
		gap: 0.75rem;
		text-decoration: none;
		flex-shrink: 0;
		transition: opacity 0.2s ease;
	}

	.logo-link:hover {
		opacity: 0.85;
	}

	.logo-img {
		height: 38px;
		width: auto;
		object-fit: contain;
	}

	.logo-text {
		font-weight: 700;
		color: var(--primary);
		font-size: 1.05rem;
		letter-spacing: 0.02em;
	}

	.nav {
		display: flex;
		gap: 0.5rem;
		align-items: center;
	}

	.nav-item {
		text-decoration: none;
		color: var(--text-muted);
		font-weight: 700;
		font-size: 0.875rem;
		padding: 0.5rem 1rem;
		border-radius: 9999px;
		transition: all 0.2s ease;
	}

	.nav-item:hover {
		color: var(--primary);
		background-color: var(--accent-bg);
	}

	/* CONTACTボタンをアクセント調に */
	.nav-item-cta {
		background-color: var(--primary);
		color: #ffffff !important;
		margin-left: 0.5rem;
		box-shadow: 0 2px 10px rgba(2, 132, 199, 0.2);
	}

	.nav-item-cta:hover {
		background-color: var(--primary-hover);
		box-shadow: 0 4px 14px rgba(2, 132, 199, 0.3);
		transform: translateY(-1px);
	}

	/* フッター構造 */
	.site-footer {
		text-align: center;
		padding: 3rem 0;
		color: var(--text-muted);
		font-size: 0.85rem;
		border-top: 1px solid var(--border);
		background: #ffffff;
		margin-top: 4rem;
	}

	.copyright {
		line-height: 1.8;
	}

	/* レスポンシブ調整 */
	@media (max-width: 640px) {
		.header-container {
			height: auto;
			padding-top: 0.875rem;
			padding-bottom: 0.875rem;
			flex-direction: column;
			gap: 0.75rem;
		}

		.nav {
			width: 100%;
			justify-content: center;
			gap: 0.25rem;
		}

		.nav-item {
			padding: 0.4rem 0.75rem;
			font-size: 0.8rem;
		}

		.nav-item-cta {
			margin-left: 0;
		}
	}
</style>
